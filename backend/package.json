{
  "name": "serverless-dashboard-infrastructure",
  "version": "0.1.0",
  "private": true,
  "description": "Serverless Framework deployment for serverless-dashboard-aws",
  "scripts": {
    "deploy": "serverless deploy",
    "deploy:dev": "serverless deploy --stage dev",
    "deploy:prod": "serverless deploy --stage prod",
    "remove": "serverless remove",
    "info": "serverless info",
    "logs": "serverless logs"
  },
  "devDependencies": {
    "serverless": "^3.39.0",
    "serverless-python-requirements": "^6.1.1"
  }
}
